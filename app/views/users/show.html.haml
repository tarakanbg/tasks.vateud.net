%p#notice= notice

.tasks_legend
  .name Name
  .status Status
  .due_date Due date
  .assignees Assignees
  .controls Actions
#tasks
  %ul.tasks
    - @tasks.each do |task|
      = render :partial => 'tasks/task', :locals => { :task => task }